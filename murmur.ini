welcometext=Launch Control
port=64738
serverpassword=propteambestteam    # shared with all clients
bandwidth=72000                # ~72kbps, plenty for voice
users=20
allowping=true

# disable default channel nesting, keep it flat
defaultchannel=0